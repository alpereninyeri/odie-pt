{
  "name": "odie-pt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "test:e2e": "playwright test",
    "release:check": "node scripts/release-readiness.mjs",
    "db:contract:check": "node scripts/db-contract-check.mjs",
    "vercel:env:check": "node scripts/vercel-env-contract-check.mjs",
    "live:smoke": "node scripts/live-smoke.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0"
  }
}
