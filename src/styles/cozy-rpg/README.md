# Cozy RPG Styles

This is the active mobile visual layer.

- `mobile.css`: cozy dusk training village UI, page backgrounds, HUD/nav, modal sheet, non-looping XP motion, and iPhone-size polish.
- Assets live in `src/assets/game/cozy/` and are Image Gen outputs optimized into cropped JPGs.

Do not reintroduce the old cyber/Hunter/mobile override stack for mobile UI work.
